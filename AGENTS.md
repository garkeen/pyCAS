# AGENTS.md — pyCAS 工程纪律

本文件规定**纪律与门限**，不重复 `cas_v4_arch.md` 的设计。对象模型、依赖方向、算法语义一律以 `cas_v4_arch.md` 为准；本文件只记 v4 未覆盖的内容，以及需要对 v4 做纪律化约束的地方。

`cas_v3_arch.md` 已被 v4 取代，保留仅供历史追溯，**不作为裁定依据**。参考实现索引见 `reference.md`（主要权威 Maxima 与 FriCAS）。

---

## 总则：实现纪律（六条硬约束，先于一切章节）

以下六条对所有代码成立，与 §零 的地基教训并列；二者冲突时以更严者为准。
**「完整实现」是本文件对实现质量的唯一验收口径**，没有"先这样、以后再补"的中间态。

1. **禁止硬编码。** 数学语义（常数、函数、规则、导数模板、定义域条件）一律以
   **数据 + 注册**表达，不得写死表、不得把同一语义散落在各消费方。这条的边界
   必须分清两类 head：

   - **数学语义 head**（`Sin`/`Exp`/`Log`/`Sqrt`/`Abs`… 等初等函数、常数）：
     其存在与语义**只能来自声明**（DSL 数据文件 + 注册）；任何模块都不得
     写死这些名字，也不得为某个具体函数加专用分支。
   - **语言签名 head**（环运算 `Plus`/`Times`/`Power`、比较 `Eq`/`Ne`/`Lt`/
     `Le`/`Gt`/`Ge`、逻辑 `And`/`Or`/`Not`、容器 `Quote`/`Piecewise` 的结构
     位置）：这是项语言自身的结构（v4 §2.1 的 AC 规范形即建立在其上），
     算法可据其分派——判等器必须认识 `Eq`，微分器必须认识 `Plus`。这类
     分派是**通用结构分派**，不是特判。

   内核不认识**任何** head（§五）；语法层只认识签名 head，不认识数学语义
   head（`Sin`/`Exp`/`Log` 一类便利构造器属声明层，不属语法层）。判据是
   「换一个函数名，这段代码要不要改」：要改就是硬编码，不用改才是通用。

2. **禁止特判与案例导向（= 禁止简化实现）。** 不得只覆盖手头案例、不得为个别
   输入加专用分支绕过通用算法、不得"先凑一个能跑的片段"。通用算法先行，快路径
   只能长在已实现的通用算法之上（§五），且必须可证明是该通用算法的子集。
   **能力缺失时诚实拒答不算简化实现**——`raise XError("未建")`、返回
   `Unknown(FRAGMENT)` 是正确行为；把未建的能力伪装成结论（伪造 Yes/No、
   逐案例堆砌冒充通用）才是禁止的。

3. **严格域分发。** 算法按域协议的**能力字段**（`is_field` / `is_ordered` /
   `is_euclidean` / 导子…）分派，不按 Python 类型、不按节点名字、不按形状嗅探
   域。域归属由显式声明与投影赋予（§一、v4 §7.6）。出现硬编码的具体域单例
   （`Q_RING`、`Z_RING`…）即违反本条，除非该算法在数学上就定义在那个域上
   （如丢番图方程定义在 ℤ 上）——此时也要以"取该能力/该域"的显式通道取用，
   不在算法体内写死。

4. **自动算法只做可判定的内容。** 自动化通道一律经 `Verdict`
   （`Yes/No/Unknown(FRAGMENT|GUARDED|UNDECIDABLE|BUDGET)`）出口，片段外诚实
   未决；`GuardPolicy` 对 Unknown 的处置永远开启（「给自动化代理的操作底线」4）。
   未决不得被静默当成 Yes 或 No。

5. **职责单一。** 一个函数/类只干一件事，且它的名字就是那件事。求解器与 checker
   分家（v4 §7.3）：checker 不得与它所验证的搜索算法同住一个模块，也不得从
   该模块导入。不同层的不同概念不得同名（如内核 `Step` 与工作流步骤记录）。

6. **名字对得起语义。** 名不副实的函数/类必须改名或改实现，不得靠 docstring
   圆场：`simplify` 不化简就不叫 `simplify`，叫 `verify_*` 的必须只做验证。

**立项自检（新函数 / 新算法 / 新规则照此逐条过）：** 语义是否已数据化？是否只
覆盖了手头案例？域是否按能力分派？不确定时是否诚实未决？职责是否单一？名字
是否就是它做的事？六问有任一为否，不立项、不落地。

---

## 零、地基教训（v1/v2 沉淀，v4 未显式收录，本文件补充）

四条用失败换来的排序纪律，先于一切章节成立。新域、新算法、新规则立项时按此自检。

1. **判等第一优先。** 判等 = 化简为规范形后判定（域协议 `normalize` → `equal`）。任何新结构的第一步是打通判等通道：判等不可机械化，就不进入计算，更不声称任何结论。判等只走域层代数运算，不靠数值采样、不靠形状猜测、不靠化简器碰运气（v2 遗留「数值采样冒充证明」即此线之下的罪）。

2. **先定域，再谈计算。** 每条数学判断的起点是「这是什么域上的命题」（ℤ、ℚ、ℚ(i)、K[x]、K(x)、…）。域归属不确定即拒答，不进入计算；域在上层由显式声明 / 投影赋予（不做叶嗅探），在算法内只查能力字段。把无序域当有序、把整环当域这类错误按能力字段拒绝，不做名字/类型推理。

3. **分段函数特殊谨慎。** 分段嵌套分段、分段微分、分段积分、分段换元（代入）一律按复杂行为处理，绝无捷径。变换后必须复查：(a) 支集在变换下是否仍互斥——「有序首中、每点至多落一支」是求值语义的地基，不得被悄悄破坏；(b) 断点/胞腔在换元、微分、积分后的**序**是否重组；(c) 定义域胞腔与值域归属是否改变。分段点（点胞腔）的可导/可积/可取性未验证就显式标注，绝不逐支冒充整体结论。

4. **直白的代数操作语义。** 计算一律降为域元素上的显式代数运算（±×÷、gcd、求导、结式、投影判等……），不做语法捷径、不做分析性逼近。代数学基础（环/域公理、欧几里得性、无平方、结式性质、主理想分解）由本系统证明或引用 `reference.md` 权威索引，无依据的操作不得入册——新算法立项前先答：「它建立在哪条代数定理上？」

## 一、数域一等公民

数域是架构主干。域协议与投影签名见 v4 §7.6。纪律：

- 上层算法按**能力字段**分派，不按类型特判
- `equal` 仅对成员有定义，片段内完全判定；非成员返回空表示调用方越界——三值属判定层，域内不产生
- 域由显式声明进入，不做叶嗅探；域归属由投影赋予，不靠猜
- 公共算法机器不得私有复制域协议方法
- **禁止 import 期修改全局状态**：域经 `install(builder)` 显式装配（v4 §7.1）

## 二、语义归数学模块

一切数学语义（常数、函数、规则、导数模板、定义域条件）归 `math/*` 模块，经 `install(builder)` 装配。v3 的 `library/` 已被取代。

**library 必须领域特定语言（DSL）声明：** 规则已有行式 DSL（`cas/math/loader.py`：`rule <id> = <pattern> -> <template> [guard <expr>] [prio N] [auto]`），常数、函数、导数模板、定义域条件同样统一进 DSL 数据文件（`math/elementary/declarations.dsl`），**禁止 Python 代码直注册**。这样准入纪律才能对 DSL 文本机械化检查，而不是靠审查 import 语句：**导数模板与定义域条件**只收无条件可证者（分支破裂者置空并记明理由）；**规则**则必须声明全部条件（见下条），两者纪律不同，不得混用一句话概括。

**工作流可用 DSL 保存与复现：** 序列化格式（未完成项，见 progress.md）以 DSL 表达步骤链——步骤、守卫、证据、分支——可回放复现同一推导，DSL 文本即推导文档。

**信任边界是「本系统不是定理证明器」的唯一机制**（v4 §10）：不可信组件可以给错候选，但只能产出 Artifact，不得直接创建可信 Judgment。可信/不可信清单见 v4 §10，此处不重复。

**规则条件取代「只收无条件恒等式」：** 规则必须声明全部条件，准入不再排除分支破裂者；能否自动应用由 `GuardPolicy` 分档（v4 §6.9）。分支破裂的改写（`tan` 需 `cos≠0`、`Log(uv)` 拆分、`√(x²)=±x`、`Exp∘Log` 主支）以**带条件规则**入册。

**导数模板与定义域条件的准入纪律不变**：只收无条件可证者（相对函数自身定义域）；分支破裂者置空并记明理由，绝不冒充无条件真理。

## 三、ADT 语义强化

- 冻结 dataclass（`frozen=True, slots=True`）表示不可变值对象
- 封闭变体层次结构表示判定结果、Verdict、CheckResult 等
- 类型层次让非法状态不可表示

**反向约束：** 封闭变体适用于结论类型与判定结果，**不适用于步骤**。`Step` 不得按功能分子类；内核不得含功能特化原语。禁止清单见 v4 §2.5 / §6.6。

## 四、性能纪律（v4 无对应条款，本节全部为本文件补充）

v4 的对象模型是证明器级严谨度，信任边界把搜索侧留成启发式。性能纪律保证这一点不被侵蚀。

**1. 热循环在 Artifact 上运行。** 化简（`rebuild` 重新驻留 + `autosimplify` 声明规则定点）、Risch 搜索、求解策略、自动 tactic 的中间产物是 Artifact（无真假，免费），不逐个 commit。可信结论只在**边界 commit 一次**。

**2. 三种执行模式，健全性等价，差别只在可追溯性：**

| 模式 | 读依赖记录 | 条件清偿 | Applicability 缓存 | 历史保留 | 用途 |
|---|---|---|---|---|---|
| `interactive`（默认） | 关 | 延迟至首次查询 | 开 | 截断 N 代 | 交互化简 |
| `derivation` | 按 Step 粒度 | 提交时算 | 开 | 完整 | 手动推导链 |
| `audit` | 按 read 粒度 | 提交时算 | 开 | 完整 | CI / 测试 |

**3. 开关只允许改变记账粒度，不得改变返回值的正确性。** checker 代码路径**不得读取任何模式标志**；模式分支只允许出现在纯记账的附加语句里（主计算在前，`if track_reads: log(...)` 之后不改变返回值）。可机械化检查。

**4. commit 特化：** v4 §6.9 的十步流程是完整算法；无前驱、无条件的典型情形走四步（scope 检查 → checker 调用 → 记录结论 → 原子写入）。特化必须可证明是完整流程的子集，不是旁路。

**5. store 分层：** Judgment store 追加式不可删；Event/Revision 历史按模式截断 N 代供 undo/redo。

**6. 驻留与化简缓存必须有淘汰策略**，不得无界增长。项 id 记忆化缓存、hash-consing 驻留表在长会话下需有边界。

**7. 三层要求，只有第一层不可放弃：**

- **健全性**（不可放弃）：hash-consing + 指针判等、Artifact/Judgment 分离、边界一次 commit、checker 返回条件且 commit 记录、`GuardPolicy` 对 Unknown 的处理
- **可追溯性**（可按模式降级）：读依赖记录、条件清偿、Applicability 计算
- **可审计性**（可丢弃）：provenance 链、完整 Event 历史、规则 tags

砍掉第二、三层不降低正确性，只降低可解释性。

## 五、内核不特判

- **内核不认识数学 head**：同余/提升规则按 head 注册到 math 模块，不得在内核遍历代码里硬编码 head 名字（通用禁特判纪律见总则 1、2，此处不重复）
- 内核不建立「关系属性注册表」——传递性、约分属 `math/base` 的规则，不是内核注册表

## 六、债务零携带

已判定的失败设计不进 v4，v3 的不允许回退。

**v2 遗留：** 驻留即规范化（构造期折叠）；叶嗅探定域；全局可变注册表跨计算泄漏；数值采样冒充证明。

**v3 遗留（v4 反向修正，逐条对照为本文件补充）：**

| v3 现状 | v4 裁定 |
|---|---|
| 守卫被推翻的步骤标 dead 并沿依赖边级联销毁 | 原结论保留，状态改 `Inapplicable`（§6.10） |
| `Derivation` 封闭 ADT 八子类 + `_verify` 按 isinstance 分派 | Step 无子类，分派走 checker 注册表（§6.6） |
| 可变 Context 列表 + clone + 位置指针撤销 | 持久化 Scope 树带父指针；undo/redo 只移 revision 指针（§6.2 / §8.5） |
| 规则验证重跑规则搜索（`_verify_rewrite` 调 `apply_rule`） | checker 不导入对应搜索算法，验证实例即可（§7.3） |
| 未知验证 → open（fail-open） | 未验证候选不能参与可信推导（§6.9 / §十二.16） |
| import 期修改全局状态（`load_all()`、`_install_base_domains()`、`register_eq_stage`） | 禁止，经 `install(builder)` 装配（§7.1） |

## 七、目录与依赖纪律

目标结构与依赖方向（含严格禁止清单）见 v4 §三 / §四，此处不重复。纪律：

- 测试不写在源码目录，数学语义不写在内核里
- **禁止 import 期修改全局状态**：域、声明、判定阶段一律经 `install(builder)` 显式装配（v4 §7.1）
- `stress/`（随机压力测试，自证数学性质，无外部真值）与 `tests/`（确定性钉子）分工不变：stress 管「数学没错」，tests 管「结构与契约没退化」
- 依赖方向与引用方向由 CI 门禁机械检查（`tests/test_v4_invariants.py`），不靠人工审查

---

## 给自动化代理的操作底线

1. 改动前先完整读将改的文件，再读 `cas_v4_arch.md` 对应章节；裁定优先于直觉与参考实现。删旧表述与旧实现，不得只加不删。
2. 新增数学语义一律落到对应 `math/*` 模块，经 `install(builder)` 注册；**不得写进内核，不得在 import 期修改全局状态**。
3. 域归属由投影赋予（v4 §7.6），不要按节点名字/类型嗅探域。
4. 判定结果用 `Verdict` ADT，理由只用 `FRAGMENT`/`GUARDED`/`UNDECIDABLE`/`BUDGET` 四种；禁止用字符串或异常吞掉未决。**`GuardPolicy` 对 Unknown 的处理永远开启——它不是性能开关，是唯一不许关的开关。**
5. 验证器与求解器必须独立：checker 不得导入对应求解算法，只能依赖微分器、域标准形、恒等判定、定义域分析这四项独立设施（v4 §7.3）。绝不自己验证自己。
6. 中间产物走 Artifact，可信结论只在边界 commit 一次；默认执行模式 `interactive`。
7. 提交前跑两步：`stress/` 与 `tests/`。**v4 §十二 的 20 条不变量，凡可机械检查者必须落成 CI 门禁。**

---

## v4 §十二 不变量的落地要求（本文件补充）

不变量 1 / 2 / 14 / 16 / 18 已落成 CI 门禁并全部转绿，另有依赖方向与引用方向门禁 4 条，全部在 `tests/test_v4_invariants.py`：

| 不变量 | 门禁 |
|---|---|
| 1 Term 无 guard/context/proof 字段 | 断言所有项变体 `__slots__` 集合 |
| 2 Pattern 不是 Term | 项层不得暴露 `PatVar`/`PatSeq`，模式通道独立成层 |
| 14 checker 不导入自身搜索算法 | 静态核验 checker 的 import 面，禁止导入求解器模块 |
| 16 未验证候选不能参与可信推导 | checker 未决 → 状态 `undecided`，不写账本 |
| 18 自动化简不应用未证明的条件规则 | 断言不存在 `auto=True and guard is not None` 的规则 |

另有三条全库机械门禁：数学语义 head 不得写成字面量；求解器模块不得内嵌验证器；声明 DSL 准入（auto 规则无守卫、分支破裂导数如实 Piecewise 且留痕、别名与角色来自声明、装配模块无写死头名）。

其余不变量随对应机制落地时一并加门禁。**「红灯先挂」只在确实需要跨阶段迁移时使用**；已经迁移到位的条目不得继续挂着红灯表述。

## `cas_v3_arch.md` 废止范围（本文件补充）

**已被取代，不作为裁定依据：** §2.3 系统骨架（工作流）、§2.4 图书馆、§3.4 注册纪律（`scoped`/`domain_scope`）、§八 路线图（重排为 v4 §十一；Risch 门控结论不变，v4 第六阶段才迁移 integration）。

**仍然有效**，作为数学事实与验收标准保留：§1.2 语言契约、§1.3 逻辑层、§1.4 验证独立性、§7.1–§7.5 判定与验证（拒答协议表、可判定性地图、Unknown 出口纪律）、§九 验收方法论。

---

## 八、终极目标与节奏

终极目标是三项，其余功能（定积分、极限级数、求和差分、ODE、不等式求解）建立在这三项地基之上：

1. **完整 Risch**（参数积分与超越数积分，参照 FriCAS）——Risch 门控结论不变：地基完工前不启动，v4 第六阶段才迁移 integration
2. **完整 CAD**——当前一维实代数胞腔只是消费 CAD 的地基；完整 CAD = 多变量实代数投影 + 胞腔分解
3. **Gröbner 基方法**——多元多项式理想论（理想属员、方程系统、代数预处理），属公共算法机器层

节奏纪律：一步步来，不急着堆功能。每步立项前用 §零 四条地基教训自检；内核（syntax/kernel/workflow 分工）先于功能生长；功能以「可判定片段 + 独立验证器」开路；进度只记在 `progress.md`，不重复进设计文档。
