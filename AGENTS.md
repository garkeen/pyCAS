# AGENTS.md — pyCAS v3 工程纪律

本文件是本项目（CAS/，纯符号计算机代数系统）对所有贡献者与自动化代理的**强制工程纪律**。任何代码、文档、提交的改动都必须遵守。设计权威与算法语义的细节见同级 `cas_v3_arch.md`；参考实现索引见 `reference.md`（主要权威 Maxima 与 FriCAS）。

---

## 一、数域一等公民

从第一天起，数域系统是架构主干。每个域自带：

- **normalize** — 域内标准形
- **equal** — 域内完全判定判等（片段内绝不 UNKNOWN）
- **member** — 成员测试

域由显式声明进入，不做叶嗅探。表达式属于哪个域由投影赋予，不是靠猜。

## 二、图书馆是唯一语义居所

任何函数、常量的定义全部走 `library/` 声明。内核（`cas/`）不携带任何数学语义：

- 打印名、正性、粗界、恒等式规则——全部在 `library/` 声明为数据
- 准入纪律：只收无条件成立的恒等式；分支破裂者一律不收
- 内核消费图书馆条目，绝不反向

## 三、ADT 语义强化

使用 Python 的代数数据类型强化语义：

- 冻结 dataclass（`frozen=True, slots=True`）表示不可变值对象
- 封闭变体层次结构（sealed class hierarchy）表示求值结果、判定三值等
- 类型层次结构让非法状态不可表示

## 四、代码清晰与高效

- 代码写清晰，可读性优先
- 同时保持高效，降低时间和空间复杂度
- 热路径避免不必要的对象分配

## 五、禁止特判

不允许各种特判或简化实现。规则：

- 通用算法先行
- 快路径只能长在已实现的通用算法之上，作为优化降低复杂度
- 不允许在通用管线中为特定函数头硬编码行为

## 六、v2 债务零携带

v2 的算法可以参考，实现不搬。已判定的失败设计不进 v3：

- 驻留即规范化（构造期折叠）
- 叶嗅探定域
- 全局可变注册表跨计算泄漏
- 数值采样冒充证明

## 七、目录纪律

- **stress/** — 所有随机压力测试，单独文件夹（自证数学性质，无外部真值）
- **library/** — 所有函数与常量定义，单独文件夹
- **tests/** — 确定性钉子测试（退化形态、修过的 bug、职责边界），与 stress 分工：stress 管"数学没错"，tests 管"结构与契约没退化"
- 测试不写在源码目录，定义不写在内核里

---

## 给自动化代理的操作底线

- 改动前先读 `cas_v3_arch.md` 对应章节，裁定优先于直觉与参考实现。
- 新增数学语义（常数、函数、规则、导数模板、定义域条件）一律落到 `library/`，不要写进 `cas/`。
- 表达式归哪个域由 `cas/project.py` 的投影阶梯决定；不要按节点名字/类型嗅探域。
- 判定结果用 `cas/verdict.py` 的 `Verdict` ADT（`Yes/No/Unknown(reason)`），理由只用 `FRAGMENT/GUARDED/UNDECIDABLE/BUDGET` 四种；禁止用字符串或异常吞掉未决。
- 验证器与求解器必须独立：求解器只交证书，验证器（回代判官、域层导数交叉复核等）另写一套，绝不自己验证自己。
- 提交前跑两步：随机性质 `stress/`（pytest 收集于 `stress/test_stress.py`）与确定性 `tests/`。
