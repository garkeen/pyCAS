# pyCAS transcript: ∫1/(a+b·cos(x)) dx —— tan(x/2) 半角换元 + 参数有理积分
#
# 经典三角有理式：t = tan(x/2) 代换把 sin(x)、cos(x) 变为 t 的有理式，
# 积分化为参数有理函数 ∫ 2/((a-b)·t²+(a+b)) dt。
#
# 人机分工：人选换元（:subst）+ 会话定理（:rule cos2atan 半角公式），
# 机器做规则化简（:auto）与参数积分（:integrate）。
#
# 判别式 D = 4(a+b)/(a-b) 在参数上符号不可判 → 结果带 proviso [D≠0]
# （atan 公式对 D≠0 任何符号成立，复对数主值组合；符号验证背书）。
# 结果留在 t 域，t = tan(x/2)。
integrate(1/(a+b*cos(x)), x)
# 惰性积分名词（不立即求值，先做换元；:subst 对 Integrate 名词解逆换元）
:subst t=tan(x/2)
# 积分变量 x → t；dx = 2/(1+t²) dt；cos(x) = cos(2·atan(t))（待半角公式展开）
:rule cos2atan = cos(2*atan(?t)) -> (1-?t^2)/(1+?t^2)
# 会话定理：双角余弦的半角有理形式
:a cos2atan
# 应用到 cos(2·atan(t)) 子项（path 全式搜索）
:auto
# 化简：合并嵌套有理式 → ∫ 2/((a-b)·t²+(a+b)) dt
:integrate
# 求值参数有理积分：Hermite 分解 + atan 闭式
#   4·atan(2t/√D)/((a-b)·√D)，D = (4a+4b)/(a-b)
#   [VERIFIED, method: Hermite reduction + RootOf log part]
#   [proviso: (4a+4b)/(a-b) ≠ 0]
