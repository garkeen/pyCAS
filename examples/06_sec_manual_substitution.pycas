# pyCAS transcript: ∫sec(x)dx = ∫1/cos(x)dx 的手动 sin-代换全程（人做数学，机器做簿记）
#
# 经典路线：1/cos = cos/(1-sin^2) -> cos dx = d(sin x) -> ∫dt/(1-t^2) -> 回代。
# 展示能力：:set 穿透绑定词的子项手术（equivalent 闸门 + 域 proviso 记账）/
#   代换定义入账（:assume，推导数学内容的一部分）/ :usub 精确微分路线
#   （cos dx = dt，不求逆）/ 账本等式背书回代（decide 归一代入消费 Eq 事实）/
#   微分回验的 PROBABLE 诚实背书。
# 注 1：凑形 cos/cos^2 在 mk 构造时即合并回 1/cos（generic 语义），故恒等变形
#   一步落到终点形 cos/(1-sin^2)；要保借用形结构用 quote 前缀（manual §2）。
# 注 2：回代 :rsub 能过闸门不是因为"随便换"，而是 t = sin(x) 已在账本——
#   没有账本等式时同样会被拒绝（诚实三态）。
I := integrate(1/cos(x), x)
I
:set 0.0 cos(x)/(1 - sin(x)^2)
:assume t = sin(x)
:usub t=sin(x)
:value
:rsub t=sin(x)
!verify % x 1/cos(x)
