[pytest]
testpaths = tests
timeout = 120
timeout_method = thread
markers =
    unit: one module, deterministic, isolated behavior
    contract: architecture invariants, dependency gates, DSL admission, export surface
    integration: multiple components exercised together
    regression: a nailed bug, kept to catch the same regression
    random: randomized self-certifying math property bench
